export interface LoggerOptions {
    /**
     * 前缀
     */
    prefix: string;
    /**
     * 是否显示类型
     */
    type: boolean;
}
export declare const defaultOptions: {
    prefix: string;
    type: boolean;
};
